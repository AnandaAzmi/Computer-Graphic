//int x = 80;
//int dx = 8;

void setup() {
  size(640, 480);
  background (127, 56,78);
}

void draw () {
  background (000);
  size(640, 480);
  ellipse(mouseX,mouseY,50,50);
  
  //x = x + dx;
  //if (x + 25 > width || x - 25 < 0) {
   // dx = dx * -1; //bisa juga dx *= -1;
  }
